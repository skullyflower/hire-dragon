import { describe, expect, it } from 'vitest';
import { render } from '@testing-library/react';
import { {{pascalCase name}} } from './{{pascalCase name}}';

describe('{{pascalCase name}} tests', () => {
  it(' should display the component name and prop.', () => {
    const { getByText, queryByText } = render(<{{pascalCase name}} thing='something' />);
    
    expect(queryByText('{{pascalCase name}}')).toBeDefined();
    expect(getByText('something')).toBeDefined();
  });
});
